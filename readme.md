Q:1) What is the difference between var, let, and const?

=>var: when a variables is declared with var it can accessible inside and outside the function

=>let: let only declered the inside the function or {} part.

=>const: cannot redeclered the value when a variable is declered by const.you can not change the the variable value

 Q:2) What is the difference between map(), forEach(), and filter()? 

=>map(): when you want to change something in a array and return with new array.

=> forEach(): forEach is used when you dont need something to return only change somethig in array

=>filter(): filter filtering the data from an array and return with new array

Q:3) What are arrow functions in ES6?

arrow function is a shorter way to write function

let arrowFunction=(paramitter)=>funtionality